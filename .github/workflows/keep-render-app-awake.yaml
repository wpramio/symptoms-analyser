name: Keep Render App Awake

on:
  schedule:
    # Runs every X minutes
    - cron: '*/13 * * * *'

jobs:
  ping:
    runs-on: ubuntu-latest
    steps:
      - name: Send HTTP Request
        run: curl -I https://symptoms-analyser-projeto-tcc.onrender.com
